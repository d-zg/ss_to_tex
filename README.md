quick cli for converting screenshots to .tex via claude. configure ss directory, api key, and model in `~/.config/latex_ocr/`.

using quick actions to shortcut this so i can screenshot and get .tex in two presses
